# 셀프호스팅용 예시 구성.
# 이 레포는 Bot 클라이언트만 포함하므로, API_BASE_URL이 가리키는
# Onyu API 서버를 별도로 준비해야 정상 동작한다 (README 참조).
services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile
    env_file: .env
    restart: unless-stopped
